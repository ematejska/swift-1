# DeviceKind

A TensorFlow device kind.

``` swift
public enum DeviceKind
```

## Enumeration Cases

### `cpu`

The CPU device kind.

``` swift
case cpu
```

### `gpu`

The GPU device kind.

``` swift
case gpu
```

### `tpu`

The TPU device kind.

``` swift
case tpu
```
